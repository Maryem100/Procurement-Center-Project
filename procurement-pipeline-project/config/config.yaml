# Configuration du Pipeline de Procurement

database:
  postgresql:
    host: postgres
    port: 5432
    database: procurement_db
    user: postgres
    password: postgres

hdfs:
  namenode: localhost
  port: 9000
  base_path: /procurement

presto:
  host: localhost
  port: 8080
  catalog: hive

paths:
  raw_orders: data/raw/orders
  raw_stock: data/raw/stock
  processed_aggregated: data/processed/aggregated_orders
  processed_net_demand: data/processed/net_demand
  output_supplier_orders: data/output/supplier_orders
  logs_exceptions: data/logs/exceptions

data_generation:
  num_products: 100
  num_suppliers: 10
  num_warehouses: 3
  num_stores: 5
  date_range_days: 7